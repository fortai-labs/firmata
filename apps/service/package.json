{
  "name": "@fortai/scraper-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cargo run --bin scraper-service",
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy",
    "migrate": "cargo run --bin migrate"
  }
} 